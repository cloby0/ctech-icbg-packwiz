# Loads block/item models on demand instead of baking every model at startup.
# Largest single heap saving on this pack (GTCEu + chipped + rechiseled + every-compat + Macaw's).
mixin.perf.dynamic_resources=true
